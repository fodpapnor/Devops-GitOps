{{- if .Values.ingress.enabled }}

apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: {{ .Values.nameOverride }}-ingress
  namespace: {{ .Values.namespace }}
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Values.namespace }}-{{ .Values.nameOverride }}-redirect-to-https@kubernetescrd,{{ .Values.namespace }}-{{ .Values.nameOverride }}-security-headers@kubernetescrd"
    argocd.argoproj.io/sync-wave: "1"
spec:
  ingressClassName: {{ .Values.ingress.className }}

  tls:
    - hosts:
        - {{ .Values.ingress.host }}
      secretName: wildcard-tls

  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: {{ .Values.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.nameOverride }}-service
                port:
                  number: {{ .Values.service.port }}

{{- end }}